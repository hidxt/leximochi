# Leximochi 安全红线（SECURITY-GUARDRAILS）

> 本文件是「词团子 / Leximochi」项目**长期生效的安全红线**，优先级仅次于 `开发提示词.md` 中的明确要求。
> 所有 Agent、子 Agent、自动化脚本、Phase 实施、数据库 migration 和 CI 步骤都必须遵守。
> **任何实现不得以「加快开发速度」「先跑通再说」「临时方案」为理由绕过本文件中的任何一条规则。**
> 若本文件与 `开发提示词.md` 冲突，以 `开发提示词.md` 为准，并立即提出修改建议（见第 14 节）。
> 若本文件与真实代码、Git 状态或测试结果不一致，以真实现状为事实来源并修正本文件。

- 文档状态：生效中
- 最近更新：2026-09-21
- 适用范围：`apps/web`、`apps/mobile`、`apps/admin`、`apps/server`、`packages/*`、脚本、部署配置、文档

---

## 0. 总则

1. **客户端数据默认不可信**：任何来自 Web、Android、管理后台、第三方回调或离线同步队列的数据都必须经过服务端校验、鉴权与业务规则判定。
2. **服务端是唯一权威**：金币、宠物 EXP、学习积分、连续学习天数、勋章/称号、复习调度只能由服务端结算与写入，客户端提交的是「事件与原始答题输入」，不是「最终数值」。
3. **最小权限**：默认拒绝。每个接口显式声明所需身份（匿名 / 普通用户 / 管理员 / 特定角色权限），禁止默认放行。
4. **默认安全**：新功能默认需要鉴权、输入校验、限流与审计；放宽限制必须有明确理由并记录在 `HANDOFF.md`。
5. **不猜、不赌**：不确定时应查文档、查现有代码、查官方文档并实际验证；禁止凭猜测写安全相关代码。
6. **失败要显式**：安全校验失败一律返回明确但**不泄露内部信息**的错误；禁止静默降级、禁止把异常当作「通过」。
7. **无秘密原则**：仓库中任何文件（含文档、测试、示例、注释、截图、日志、commit message）都不得出现真实密码、Token、API Key、主密钥、恢复码或真实 `.env`。

---

## 1. 秘密信息与配置文件

- **禁止提交真实 `.env`**：仓库只允许提交 `.env.example`（仅含占位符与说明）。`.env`、`.env.*`（除 `.env.example`）、`*.key`、`*.pem`、`*.p12`、`*.keystore`、`*.jks`、`google-services.json`、`credentials.json` 必须写入 `.gitignore`。
- **主密钥管理**：AI API Key 加密主密钥、JWT 签名密钥、Refresh Token 派生密钥必须来自环境变量或仓库外的独立密钥文件（文件权限受限、路径不入 Git）。禁止硬编码、禁止默认值兜底、禁止在测试中写真实密钥（测试用临时生成的固定假值）。
- **启动即校验**：服务端启动时若缺少必需密钥或密钥强度不足（如长度低于约定下限、等于示例占位值），必须**拒绝启动**或进入显式的不安全模式并告警，禁止静默使用弱默认值。
- **密钥轮换预留**：加密数据需携带密钥版本标识（如 `keyId`/`v1:` 前缀），以便未来轮换主密钥而不丢失数据。
- **提交前扫描**：每次提交前检查暂存区内容，禁止把 `.env`、密钥、Token、数据库文件、`data/`、日志一并提交；对疑似秘密文件即使文件名无害也要先读内容确认。
- **日志与错误输出**：日志、异常堆栈、审计记录、后台页面、监控/遥测中禁止出现完整 API Key、密码、恢复码、Token、Cookie、Authorization 头、数据库连接串、SQLite 绝对路径、服务器文件结构与环境变量内容。
- **对外错误响应**：禁止返回内部堆栈、SQL 语句、SQLite 路径、文件系统结构、内部类名/方法名、依赖版本明细。

### 1.1 AI API Key 的加密存储与使用

- **必须可逆加密（AEAD）**：用户自行配置的 AI API Key 必须使用 AEAD 认证加密（如 AES-256-GCM 或 XChaCha20-Poly1305）加密后入库，以支持 Web/Android 多端同步。**禁止**明文存储，也**禁止**只做哈希或 Base64/编码混淆（哈希不可逆，无法满足同步与调用需求）。
- **每次加密使用独立随机 nonce**，密钥与 nonce 不得复用；密文需携带算法与密钥版本（`keyId`），以支持未来轮换主密钥而不丢数据。
- **主密钥隔离**：加密主密钥来自环境变量或仓库外的独立密钥文件，强度足够且**禁止写入 Git、禁止写入日志、禁止下发给客户端**；缺失或为示例占位值时服务端拒绝启动。
- **解密只在服务端**：明文 Key 仅在服务端发起外部调用时在内存中短暂存在；禁止下发到 Web/Android，禁止写入队列、缓存、任务参数、临时文件或前端状态管理。
- **回显一律掩码**：接口与后台只能返回掩码（如 `sk-****abcd`）；更新 Key 必须有明确语义（显式传新值才覆盖，空值表示「保持不变」），**禁止**把掩码值当真实值回传覆盖，导致用户密钥被写坏。
- **管理员也不得看明文**：后台任何页面、导出、审计日志均不得展示用户 Key 明文。
- **日志与错误脱敏**：连接测试、Provider 报错、出站请求日志可能包含 Key 片段，必须统一脱敏（禁止记录 `Authorization` 头与请求体中的 Key 字段）；错误信息返回给前端前同样要脱敏。
- **连接测试必须分能力**：文本 / STT / TTS / Realtime 必须分别测试并分别报告结果，**禁止**只测通一个接口就显示「全部正常」；不支持的 Provider 能力需明确提示而非静默失败。
- **归属与隔离**：每个用户的 Key 只能用于该用户自己的请求，禁止跨用户复用、禁止被平台自身调用；校验调用方身份后再取用所属 Key。
- **彻底删除**：用户删除配置或删除账号时，必须真正清除密文及相关缓存，禁止只做软删除标记而保留可用密文。

---

## 2. 账号、密码与恢复码

- **密码哈希**：只允许成熟的内存硬哈希算法（首选 Argon2id，参数需显式配置且不低于业界常见下限；备选 bcrypt/scrypt 需说明理由并记录）。**严禁** MD5、SHA1、裸 SHA256、加盐单轮 SHA 系列、自研算法、明文或可逆加密存储密码。
- **密码策略**：设置长度与复杂度下限，禁止服务端静默截断；登录失败不区分「用户不存在」与「密码错误」的提示文案（防用户名枚举）。
- **恢复码**：注册时生成一次性恢复码，仅以安全哈希形式入库（等价于密码哈希标准），明文只在生成当次返回给用户且永不落库、永不写日志。使用后立即失效；每次使用后重发/轮换策略必须显式设计；恢复码不得用于绕过限流或人机验证。
- **恢复流程安全**：恢复码校验必须限流、记录审计事件；恢复成功后强制撤销该账号全部 Refresh Token/设备会话。
- **人机验证**：注册、登录、找回等敏感入口必须经过 `CaptchaProvider` 抽象校验；Provider 校验结果必须在服务端二次确认，禁止信任客户端上报的「已验证」标记。降级策略（如 Provider 不可用）必须显式设计且不得无限放行。
- **注册/登录限流**：按账号、IP、设备维度限流并支持风控规则；命中风控要记录审计，禁止只在前端做限制。

---

## 3. 认证、会话与 Token

- **Access / Refresh Token 设计**：Access Token 短期有效且服务端可校验签名与过期；Refresh Token 长期有效但必须支持**轮换（rotation）**、撤销、绑定设备会话、过期时间与复用检测（旧 token 被重复使用时撤销该会话链）。
- **存储**：Web 端优先使用 `HttpOnly` + `Secure` + `SameSite` Cookie 承载会话类凭证，避免把长期凭证放入 `localStorage`；Android 端使用系统安全存储（Keystore 支持的能力），禁止明文落盘。
- **退出登录**：必须服务端撤销会话，而非仅清除本地 token；支持「退出全部设备」与设备会话列表管理。
- **越权前置校验**：所有需要身份的接口在业务逻辑之前完成鉴权；禁止「先查数据再判断归属」的写法造成信息泄露。
- **角色与权限变更即时生效**：封禁、降权、撤销会话后，旧会话必须尽快失效，禁止依赖「token 自然过期」。
- **敏感操作**：修改密码、生成/使用恢复码、修改 AI 配置、删除账号、管理员高风险操作需要重新校验身份或二次确认，并写入审计日志。
- **权限判断只看服务端**：前端隐藏按钮不构成权限控制。

---

## 4. 授权、RBAC、管理员与 IDOR

- **权限严格分离**：管理员与普通用户是不同身份域。普通用户即使获得后台 URL 或接口路径，也必须无法调用任何后台 API（403/404，且不泄露该资源是否存在）。
- **RBAC 强制**：后台每个接口都要声明所需权限点，由服务端守卫统一校验；禁止在 Controller/Service 中散落「前端已判断」的隐含假设。
- **IDOR 防护**：任何以 ID 访问资源的接口都必须校验资源归属或访问权限（用户自己的学习记录、笔记、口语报告、AI 配置等）。禁止用自增 ID 直接信任前端传入的归属。
- **管理员审计**：查询、封禁/解封、词库与内容 CRUD/批量导入、宠物与资源配置、奖励规则修改、公告、系统配置、权限变更、资源删除都必须写入管理员操作审计日志（操作者、时间、对象、变更摘要、结果）。
- **高风险操作二次确认**：涉及金币/EXP/积分发放与回滚、用户状态、权限、配置、批量导入与资源删除的操作必须二次确认，且必须由服务端校验权限。
- **禁止调试后门**：不得保留任何「调试接口」「隐藏管理员入口」「万能测试 token」「跳过验证码开关」进入生产代码。若必须存在，只能通过显式环境变量在非生产环境启用，并有测试保护其不会在生产生效。

---

## 5. 注入类与 Web 安全（XSS / CSRF / SSRF / SQLi）

- **SQL 注入**：只允许参数化查询 / ORM 安全 API。禁止字符串拼接 SQL、禁止把用户输入拼进 `ORDER BY`、`LIMIT`、表名或列名；动态排序字段必须走白名单映射。
- **XSS**：默认不渲染 HTML 字符串；必须渲染富文本时走白名单净化（服务端或可信库），并配合 CSP。禁止把用户内容塞进 `innerHTML`、`dangerouslySetInnerHTML`、`eval`、动态 `Function`。
- **CSRF**：基于 Cookie 的会话必须启用 CSRF 防护（SameSite + CSRF Token 或等效机制），状态变更接口禁止使用 `GET`；校验 `Origin`/`Referer` 白名单。
- **CORS**：只允许明确白名单来源，禁止 `*` 与 `credentials: true` 组合；生产环境禁止宽松正则匹配来源。
- **SSRF**：AI Provider 的 `Base URL`、Webhook、远程资源地址等由用户提供的 URL，必须校验协议（仅 http/https）、解析后 IP 是否属于内网/回环/链路本地/元数据地址段并拒绝，限制重定向跳数，禁止访问 `file://`、`gopher://` 等协议。禁止服务端按用户输入任意抓取内容。
- **安全响应头**：Web/Admin 至少设置 CSP、`X-Content-Type-Options: nosniff`、`Referrer-Policy`、`X-Frame-Options`/`frame-ancestors`、HSTS（生产 HTTPS 下）。
- **原型污染与反序列化**：拒绝未校验的对象合并；DTO 校验必须开启白名单模式（如 `forbidNonWhitelisted`），禁止 `any` 直接入库。
- **整数与边界**：金币、EXP、积分、时长、分数等数值必须做类型、范围、上下限校验，防止溢出/负数/超大值；所有奖励计算要有上限约束。

---

## 6. 文件上传、存储与目录穿越

- **多层校验**：上传必须同时校验文件大小上限、扩展名白名单、**真实 MIME（读取文件头/魔数，而非信任 Content-Type）**、图片/音频解码可行性。
- **文件名与路径**：禁止使用用户提供的文件名落盘（服务端生成随机名 + 安全扩展名）；禁止 `..`、路径分隔符、绝对路径、空字节、Unicode 混淆等目录穿越手段；写入前对最终路径做规范化并断言其仍位于允许的存储根目录内。
- **存储隔离**：`data/` 目录按数据库、`uploads`、`audio`、`pets`、`backups`、`logs` 分离；静态资源目录禁止执行脚本，禁止把可上传目录直接暴露为可执行/可直接解析的静态目录；上传内容一律以附件/受限 MIME 提供。
- **`StorageProvider` 抽象**：业务代码禁止硬编码绝对路径，必须通过 Provider 访问文件；`LocalStorageProvider` 与未来 `S3StorageProvider` 的接口契约需一致（试读、写入、删除、存在性、URL 签名策略）。
- **下载/读取鉴权**：非公开文件（用户上传、口语报告音频等）下载必须鉴权并校验归属，禁止可猜测的公开直链。
- **资源清理**：删除业务对象时同步处理关联文件，避免孤儿文件；批量删除属于高风险操作（见第 4 节）。
- **素材入库**：任何第三方图片/音频/Sprite 入库前必须确认许可证允许项目用途并在许可证登记文件中留痕（见第 11 节）。

---

## 7. 限流、暴力破解与滥用防护

- **限流维度**：登录、注册、恢复码、验证码、AI 调用与连接测试、上传、同步接口、后台登录必须限流（按账号 + IP + 设备/指纹组合）。限流实现要能在进程重启后保持一致或明确说明其局限。
- **防暴力破解**：连续失败需递增延迟/临时锁定，并写入风控与审计；锁定策略不得让攻击者通过该接口枚举用户是否存在。
- **防枚举**：注册、找回、用户名可用性检查等接口的响应时间与文案不得泄露账号是否存在。
- **资源滥用**：AI 相关接口（连接测试、实时语音、报告生成）必须限制并发、单次时长、请求频率与请求体大小；平台不承担用户模型费用，因此禁止无限制转发。
- **同步与幂等接口**：批量同步接口必须有批量上限、请求体大小上限与频率限制，防止以同步为名的数据洪泛。
- **成本与超时**：所有外部调用（AI、STT、TTS、Realtime）必须设置超时、重试上限与失败熔断，禁止无限重试（同样适用于 Git/网络操作，见第 13 节）。

---

## 8. SQLite、事务、Migration、备份与数据完整性

- **SQLite 配置**：必须启用 WAL 模式、设置合理的 `busy_timeout`、启用外键约束；写入使用事务，避免长事务与跨请求事务。
- **连接与访问层解耦**：业务层不得直接依赖具体 ORM 细节；数据访问集中在 repository/ORM 层，确保未来迁移数据库时不必重写业务逻辑。
- **Migration 规则**：
  - 所有结构变更必须通过版本化 migration，禁止手工改库（手动改库后的结构变更同样必须补 migration，否则视为缺陷）。
  - migration 必须可重复、可追溯，且不得包含明文秘密与真实数据。
  - 破坏性变更（删表、删列、改类型、丢失数据）必须显式说明影响并保留备份与回滚路径。
  - 已发布并经用户使用的 migration 禁止直接修改其内容或重排顺序，只能追加新 migration。
  - migration 执行前后须验证数据完整性（行数、关键约束、外键一致性）。
- **事务与幂等**：金币、EXP、积分、连续学习、勋章、复习调度、同步落地等更新必须在单个事务内完成，并使用唯一约束（如 `eventId` 唯一索引）作为幂等的最后防线；禁止「先查后写」式去重。
- **时间**：服务端统一以 UTC 存储时间，客户端按本地时区展示；所有服务端时间戳由服务端生成，禁止采信客户端时间作为业务判定的唯一依据（尤其是每日目标/连续学习的结算日归属）。
- **备份**：
  - 提供数据库备份机制（含 WAL 一致性处理，如使用 SQLite 在线备份 API 或安全 checkpoint 后复制），备份文件写入 `data/backups`，权限受限，禁止入库 Git。
  - 提供恢复流程文档并**实际演练**记录结果；备份未验证可恢复 = 没有备份。
  - 备份与导出产物不得包含明文秘密。

---

## 9. 经济系统与奖励结算（金币 / EXP / 学习积分 / 连续学习 / 勋章）

- **服务端唯一权威**：金币、宠物 EXP、学习积分、连续学习、勋章/称号的最终数值只能由服务端根据「可验证的学习事件」结算；客户端提交的数值、时长、正确率仅作为**待校验输入**，必须做合理性校验（时间窗口、速率上限、与题目/内容的一致性）。
- **四套系统独立**：学习积分、金币、宠物 EXP、勋章互不串用公式；禁止用客户端上报直接决定任何最终奖励。
- **RewardService + Reward Ledger**：所有奖励发放必须经过统一 `RewardService` 并写入奖励流水（谁、何时、因何事件、多少、使用的幂等键、是否已发放、余额变化）。禁止任何绕过 Ledger 的直写金币/EXP 代码路径。
- **幂等与防重复**：每个学习事件必须携带唯一幂等 ID；Ledger 以幂等键 + 唯一索引保证同一事件只结算一次；网络重试、离线重复同步、并发请求、浏览器/App 重放都不得造成重复发放。
- **防重放**：同步与结算接口需校验事件时间窗口、客户端签名/凭证、重复 eventId、单用户速率上限；异常时拒绝并审计，而不是「宽松放行」。
- **禁止扣减既有资产**：连续学习中断只影响连续天数与宠物心情，**禁止**扣除已有金币、EXP、等级、已得勋章。「宠物不会死亡/逃跑/掉级」，任何负反馈不得以删减用户资产实现。
- **上限与审计**：单次/单日奖励设上限；结算失败必须可观测（日志 + 审计），但日志不得泄露秘密。
- **可回溯**：奖励与结算结果必须能从 Ledger 与学习记录回溯解释，禁止出现「余额对不上算法」的黑盒状态。

---

## 10. Android 离线与同步安全

- **本地存储**：本地缓存与队列使用 SQLite 或等效可靠存储，敏感内容（Token、AI 配置相关凭证）不得明文落盘；登出时按设计清理本地敏感数据。
- **事件模型**：每个离线事件必须具备全局唯一 `eventId`、客户端时间、事件类型、版本号与必要的业务数据；事件结构需支持未来扩展而不破坏旧客户端。
- **同步接口安全**：
  - 服务端对每个事件做鉴权、归属校验、幂等校验、时间窗口校验与业务规则校验；禁止批量信任客户端提交。
  - 服务端返回**权威结算结果**，客户端以服务端结果覆盖本地展示，禁止本地先加后补。
- **乱序与冲突**：必须处理乱序到达、重复上传、服务端状态已变化（如词库版本更新、任务/规则变更）、同一账号多设备并发；冲突解决规则要显式定义并测试（服务端权威、按事件时间与版本合并、必要时保留待人工处理状态）。
- **重放与伪造**：考虑客户端被篡改（改时间、改数值、伪造 eventId、重放旧包）；服务端以速率上限、合理性校验与幂等约束兜底。
- **离线边界**：明确离线可用范围（已下载词库、新词学习、复习、必要学习记录）与必须联网的能力（AI 口语、人机验证、依赖在线资源的功能），离线时对这些能力给出明确提示，禁止静默失败。
- **词库下载**：下载内容需校验完整性与来源（哈希/签名校验），防止中间人替换；下载包不得包含可执行代码。

---

## 11. 依赖、供应链与素材许可证

- **依赖来源**：仅从官方或项目已确认的注册源安装；引入新依赖前评估必要性、维护活跃度、许可证、安装体积与已知漏洞；禁止引入来源不明、长时间未维护、疑似恶意或与产品无关的包。
- **锁文件**：必须提交锁文件并在 CI/本地使用锁定安装（如 `npm ci`）；禁止随意删除锁文件或强制升级主版本；禁止 `npm install --force`/`--legacy-peer-deps` 掩盖真实冲突而不记录原因。
- **镜像与代理**：使用第三方镜像（如 npm/maven 镜像）属于供应链决策，必须以项目范围配置并记录在 `HANDOFF.md`，不得静默改写全局配置；优先使用官方源 + 项目内代理配置。
- **依赖审计**：定期与发布前执行依赖漏洞审计（如 `npm audit`），对高危漏洞给出处置结论（升级/替换/接受风险及理由）。
- **安装脚本**：审查依赖的 `postinstall` 等生命周期脚本风险；禁止执行来源不明的安装脚本。
- **许可证**：项目许可证为 Apache-2.0（见 `LICENSE`）。所有第三方素材（图片、Sprite、音频、字体、词典数据、听力内容）必须**许可证明确且允许项目用途**，并在许可证登记文档中记录来源、作者、许可证类型、获取链接与用途。许可证不明或不允许商用的资源禁止进入仓库。
- **内容版权**：禁止未经确认直接抓取、复制或打包版权状态不明的 CET 真题/听力资源；AI 生成内容与可靠词典数据必须区分标注，AI 内容不得覆盖基础词典数据。
- **发布物**：发布包中不得包含测试密钥、调试配置、内部文档、开发用账号或未授权素材。

---

## 12. 禁止事项（恶意与越界行为）

项目**永久禁止**：

- 广告 SDK、挖矿代码、后门、RAT、键盘记录、提权/持久化组件。
- 隐蔽遥测、未在文档中说明的数据收集、与产品功能无关的后台网络请求或定时外联。
- 未经用户同意上传用户文件、剪贴板、通讯录、位置、设备标识等隐私数据。
- 植入第三方追踪、指纹识别或跨站追踪脚本。
- 任何形式的混淆代码用于规避审计（可读性优先；必要压缩构建产物除外）。
- 使用本项目代码对第三方系统进行未授权测试、扫描、攻击或数据抓取。
- 以「调试」「临时」「为了通过测试」为名关闭安全校验、跳过幂等、绕过鉴权、关闭 WAL/事务。

---

## 13. 环境安装与 Git 危险操作约束

### 13.1 软件与工具安装

- 允许：项目目录内通过 `npm` 安装正常项目依赖（React、React DOM、React Native、NestJS、Vite、测试与构建工具等）。
- **必须先申请并获得用户明确批准**（先说明缺什么、为什么需要、计划安装的软件与具体版本、来源、用途、磁盘/系统影响、是否有替代方案）：pnpm、Bun、Yarn、Python 包、Docker、Visual Studio、CMake、新 JDK、额外 Android SDK 平台/系统镜像/构建工具、模拟器镜像、全局 CLI、系统级包管理器（winget/Chocolatey/Scoop）、任何安装器或下载脚本。
- 未经批准禁止使用 winget、Chocolatey、PowerShell 下载脚本或安装器安装额外工具；禁止静默安装系统依赖。
- 默认优先使用已有工具：`npm`（含 workspaces）优先于额外包管理器。
- 系统级或全局配置（Git 代理、npm 全局 registry、环境变量、防火墙、证书）不得擅自永久修改；优先使用当前命令/当前进程/当前仓库范围配置。

### 13.2 Git 操作

- 禁止未经用户明确授权执行：`git reset --hard`、`git push --force`/`--force-with-lease`、重写历史（rebase 已推送提交、`filter-branch`、`commit --amend` 已推送提交）、删除分支/标签、`git clean -fdx`、批量删除仓库文件。
- 破坏性操作前必须先 `git status` 确认工作区状态，发现未提交改动或陌生文件时先保护（提交/暂存/备份）再继续。
- 禁止使用 `--no-verify` 跳过钩子、禁止跳过签名（除非用户明确要求）。
- 禁止提交秘密文件、数据库文件、`data/`、日志、构建产物；暂存后必须复核 `git status` 内容。
- 提交信息使用 Conventional Commits，保持提交小而聚焦。
- **GitHub 网络操作**：`clone`/`fetch`/`pull`/`push`/GitHub API 优先使用代理 `127.0.0.1:10808`（如 `-c http.proxy=http://127.0.0.1:10808` 或当前命令范围）；代理不可用或协议不兼容时自动切换直连；代理与直连均失败时报告实际错误与已尝试方法，**禁止无限重试**。本地 `commit` 不需要网络，正常执行。
- 禁止擅自修改 Git 配置（`git config` 全局项、user 信息、签名配置）。

---

## 14. 文档维护与规则变更

- 本文件必须随架构与威胁模型变化同步更新；安全要求变化时，`开发提示词.md` 与本文件需保持一致。
- 修改 `开发提示词.md`：**不得自行修改**。发现技术错误、过时内容、内部冲突、不可实现要求或更可靠替代方案时，必须说明「原规则 / 问题 / 依据 / 不修改的影响 / 建议内容 / 影响范围」，等待用户明确批准后再改；涉及安全要求时同步更新本文件。
- `HANDOFF.md` 必须保持为当前状态快照，且与实际代码、Git 状态、真实测试结果一致；禁止在没有实际证据的情况下写「已完成」「测试通过」。
- 若本文件与实际代码/测试结果冲突，以真实情况为准并修正文档，同时在 `HANDOFF.md` 记录差异。

---

## 15. 安全检查清单

### 15.1 开发前（每个任务 / 每个 Phase 开始前）

- [ ] 已阅读与当前任务相关的 `开发提示词.md`、`SECURITY-GUARDRAILS.md`、`HANDOFF.md`。
- [ ] 已确认本次改动涉及的信任边界（匿名 / 用户 / 管理员）与数据流。
- [ ] 已确认是否需要 migration、是否需要幂等键、是否需要审计日志、是否需要限流。
- [ ] 已确认不需要安装新的系统级工具；若需要，已先向用户申请并获批准。
- [ ] 已确认不引入来源不明或与需求无关的依赖与素材。

### 15.2 提交前（每次 commit）

- [ ] `git status`/暂存区复核：无 `.env`、密钥、Token、数据库文件、`data/`、日志、构建产物。
- [ ] 无硬编码秘密；新增配置项已同步到 `.env.example`（仅占位符）。
- [ ] 新增/修改接口均有鉴权、DTO 运行时校验、归属（IDOR）校验。
- [ ] 涉及金币/EXP/积分/连续学习/勋章的改动走 `RewardService` + Ledger，具备幂等键与唯一约束，写入在事务内。
- [ ] 新增 SQL 全部参数化；新增用户输入渲染无 XSS 风险；新增外部 URL 有 SSRF 校验。
- [ ] 日志与错误信息无秘密、无内部路径/堆栈泄露。
- [ ] 已实际运行 `lint` / `typecheck` / 相关测试，并在报告中给出真实输出摘要（禁止无证据声称通过）。
- [ ] 已复核依赖与素材许可证；无新增未授权资源。
- [ ] 文档（`HANDOFF.md`，必要时架构/API 文档）已更新且与实际实现一致。

### 15.3 发布前（V1 及每个正式发布）

- [ ] 完整安全审计：认证绕过、权限绕过/IDOR、SQLi、XSS、CSRF、SSRF、文件上传与目录穿越、暴力破解、API Key 泄漏、日志泄密、客户端伪造学习结果、奖励重复提交、离线同步重放、整数边界、管理员权限、危险网络请求、供应链风险。
- [ ] 依赖漏洞审计（如 `npm audit`）与处置结论；第三方素材许可证核查完成。
- [ ] 全部秘密来自环境/密钥文件；生产启动校验通过；示例配置无真实值。
- [ ] SQLite：WAL、`busy_timeout`、外键、事务、migration 全部到位；备份已实际演练恢复并记录结果。
- [ ] 限流、风控、审计日志在生产配置下生效；错误响应不泄露内部信息。
- [ ] 无广告 SDK、挖矿、隐蔽遥测、无关后台网络行为、调试后门与测试账号。
- [ ] Android/Web 发布物不含测试密钥、调试开关与未授权素材；后台权限在生产环境实测无法被普通用户访问。
- [ ] 测试/lint/typecheck/build 全部实际执行并记录真实结果；已知风险与遗留项已记录在 `HANDOFF.md`。

---

## 16. 违规处理

发现任何违反本文件的实现（无论是否已提交）时：

1. 立即停止推进新功能，用 `superpowers:systematic-debugging` 方式定位根因；
2. 评估影响面（是否已产生数据、是否已发放奖励、是否已泄露秘密）；
3. 修复并在 `HANDOFF.md` 记录问题、影响、修复方式与验证证据；
4. 如涉及秘密泄露，按「假定已泄露」处理：轮换密钥、撤销会话、必要时通知用户；
5. 不得通过掩盖、删除证据或降低校验来「消除」问题。
