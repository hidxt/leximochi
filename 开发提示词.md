你现在是本项目的首席全栈开发 Agent，请从零开始设计并实现一款“英语学习 + 宠物陪伴养成”的正式产品。项目中文名暂定“词团子”，英文名“Leximochi”，仓库和内部 package 命名优先使用 `leximochi`。GitHub Description 建议使用：`Gamified English learning with vocabulary, AI speaking, listening practice, and a virtual pet companion.`。项目同时提供 Web 网页版和 Android App，未来预留 Windows 客户端扩展能力。不要一次性堆完所有功能，必须严格采用阶段式开发，每阶段完成设计、编码、测试、安全检查和验收后再进入下一阶段。



【工作目录与 Git】当前工作目录固定为当前 `git` 目录，该目录就是项目 Git 仓库根目录，所有源码、文档、配置、测试和项目文件必须位于当前 Git 仓库中，禁止在其他目录重新创建另一套项目。开始开发前先检查当前目录、`git status`、分支、remote、已有文件、README、AGENTS.md、CLAUDE.md、docs、package.json、锁文件、数据库文件和现有代码，不得破坏已有 Git 历史或覆盖用户已有成果。提交使用清晰的 Conventional Commits，每个阶段和重要功能形成小而完整的提交，不要一次提交大量互不相关的修改。不得擅自强制 push、reset --hard、删除分支或重写历史。



【GitHub 网络规则】本地 `git commit` 不需要网络，正常执行即可。涉及 GitHub 网络操作，例如 clone、fetch、pull、push、GitHub API 或下载 GitHub 资源时，优先尝试代理 `127.0.0.1:10808`。不要擅自永久修改系统级或全局 Git 代理配置，优先使用当前命令、当前进程或当前仓库范围代理。如果该代理无法连接、协议不兼容或实际测试不可用，则自动切换为直连 GitHub，不要因为代理不可用阻塞开发。如果代理和直连均失败，再向用户报告实际错误和已尝试方法，禁止无限重试。



【本机开发环境和安装权限】当前电脑为 Windows。用户已经安装 Android Studio、Android SDK、JDK、Node.js、Git 等主要环境，但你必须实际检查，而不是直接相信环境一定可用。开工前检查至少包括：`node -v`、`npm -v`、`java -version`、`javac -version`、`git --version`、`adb version`、Android SDK 路径、Gradle/Android 构建所需组件和版本。React 当前没有单独安装，这是正常情况，React 不需要全局安装。React、React DOM、React Native、NestJS 和项目正常 npm 依赖允许你在项目目录中通过 npm 安装，不需要额外询问用户。除正常 npm 项目依赖外，如果缺少任何需要下载安装的开发工具、系统软件、SDK、CLI、运行环境、包管理器或外部程序，例如 pnpm、Bun、Python、Docker、Visual Studio、CMake、新 JDK、额外 Android SDK 平台或其他工具，禁止擅自下载或安装。你必须先告诉用户：当前检测到缺少什么、为什么需要、计划安装什么软件和具体版本、来源、用途、磁盘/系统影响以及是否存在不安装它的替代方案，然后停止并等待用户明确命令后才能安装。未经许可禁止使用 winget、Chocolatey、PowerShell 下载脚本、安装器或其他方式安装额外工具。默认优先使用用户已有的 npm，不要因为个人偏好强制安装 pnpm、Bun、Turborepo 等工具。如果 npm workspaces 足够，就优先使用 npm workspaces。



【产品定位】Leximochi 不是单纯四六级刷题工具，而是一款“英语学习 + 宠物陪伴养成”产品。核心循环为：学习英语 → 获得学习积分/金币/宠物经验 → 宠物成长 → 解锁动作/勋章/称号 → 继续学习。产品主要服务 CET-4、CET-6 学习，同时兼顾真正的英语能力提升。V1 面向正式用户开放，但不做好友、排行榜、私聊、社区、动态、金币充值和真实货币交易。金币只能通过学习、签到、成就等行为获得，不能购买。



【技术架构】主要语言统一使用 TypeScript。采用 Monorepo。建议目录：`apps/web` 为 React 用户 Web；`apps/mobile` 为 React Native Android；`apps/admin` 为独立 React 管理后台；`apps/server` 为 NestJS 服务端；公共代码放入 `packages/`，建议包括 `core`、`types`、`api-client`、`auth`、`ai`、`sync`、`shared` 等，但允许根据实际工程质量调整。Web 和 Admin 可使用 React + Vite。Android 使用 React Native，未来预留 React Native Windows 或其他 Windows 实现，但 V1 不开发 Windows。禁止为了代码复用强行让 Web 和 React Native 共用所有 UI，优先共享业务逻辑、类型、API SDK、学习算法和设计 Token，平台 UI 根据各自交互习惯实现。



【后端与数据库】后端使用 NestJS。服务端数据库使用 SQLite，不使用 Redis。ORM 可在 Prisma 和 Drizzle 中根据 SQLite 支持、迁移稳定性和项目复杂度选择一个，并把选择原因记录到架构文档。SQLite 必须启用 WAL、合理 busy timeout、事务、数据库 migration 和备份机制。业务层和数据库访问层要解耦，避免未来迁移数据库时重写全部业务。服务端直接使用 Node.js 运行，不依赖 Docker。Linux 生产部署后续提供 systemd 示例。所有时间服务端统一使用 UTC 保存，客户端按本地时区显示。



【文件存储】默认将文件保存到服务器本地独立数据目录，但必须设计 `StorageProvider` 抽象，未来可以切换 S3 兼容对象存储。建议 data 目录将数据库、uploads、audio、pets、backups、logs 分开。业务代码不得硬编码绝对路径。实现 `LocalStorageProvider`，并定义未来 `S3StorageProvider` 所需接口。上传文件必须检查文件大小、扩展名、真实 MIME、路径安全和文件名，禁止目录穿越、任意路径写入和危险文件执行。



【账号系统】V1 使用“用户名 + 密码”注册和登录，不要求邮箱和手机号。注册和登录需要人机验证码，验证码做成 Provider 抽象，以便未来接 Cloudflare Turnstile 等服务。密码必须使用成熟密码哈希算法，例如 Argon2id，禁止 MD5、SHA1、裸 SHA256 等直接保存密码。忘记密码使用恢复码机制：注册完成时生成一组一次性恢复码，用户自行保存；数据库不得保存恢复码明文，只保存安全哈希；使用后的恢复码立即失效。认证需要合理的 Access Token + Refresh Token 或等效安全设计，包含 refresh token 轮换、退出登录、设备会话管理、限流、暴力破解防护、输入校验和敏感操作审计。



【管理员系统】`apps/admin` 必须是独立 React 项目，与普通用户 Web 分离。管理员和普通用户权限严格分离，后台使用 RBAC。普通用户即使知道后台 URL 也不能访问任何后台 API。后台至少包括：用户查询、封禁/解封、学习数据概览、词库和词条 CRUD/批量导入、听力内容和音频管理、宠物配置、Sprite 资源配置、等级配置、金币奖励规则、勋章/称号管理、公告、登录/注册风控、系统配置、AI Provider 异常统计、基础运营统计和管理员操作审计日志。涉及金币、用户状态、权限和资源删除等高风险操作需要二次确认。权限必须由服务端再次验证，不能只靠前端隐藏按钮。



【首页与导航】普通用户一级导航固定为：首页、单词、口语、听力、我的。UI 整体走活泼、现代、适度游戏化方向，可以参考多邻国的轻松学习氛围，但禁止复制其品牌、角色、素材、文案或界面。首页采用“宠物 + 今日学习任务”混合布局：上部显示宠物 Sprite、等级、EXP、心情、饱食度和金币；中部显示今日学习积分、目标和连续学习天数；下部显示单词、复习、口语、听力任务和快捷开始按钮。



【学习计划和积分】用户自行设置每日学习目标，而不是系统强制分配。至少包括每日新词数量、复习目标、口语分钟数、听力分钟数。学习积分采用“基础分 + 少量难度/表现加成”。学习积分、金币、宠物 EXP、勋章必须是四套独立系统：学习积分用于判断今日学习完成度；金币用于宠物消费；EXP 用于宠物升级；勋章代表长期成就。达到每日学习积分阈值才能保留连续学习记录；未达到则连续学习中断、宠物心情小幅下降，但禁止扣除已有金币、EXP 和等级。



【宠物系统】V1 只有一只默认宠物，使用 2D Sprite 序列帧，不使用 Live2D。至少提供 `idle`、`study`、`eat`、`happy`、`sad`、`sleep`、`levelup` 等动画状态。宠物拥有等级、EXP、心情值和饱食度。饱食度随现实时间缓慢下降，心情主要受学习完成度、连续学习和互动影响。宠物不能死亡、逃跑、掉级，不允许通过极端负反馈制造压力。宠物升级主要依赖学习获得的 EXP，喂食不能刷等级。升级后 V1 可以解锁表情和动作，同时数据结构预留未来装扮、玩具、房间和特殊互动。宠物资源不得把图片路径写死在业务代码，建立 Pet/AnimationSet 配置系统。允许使用 itch.io 等来源的素材，但只有许可证明确、允许项目用途的素材才能进入仓库，必须记录来源和许可证。



【金币和奖励】金币不能充值或购买，只能通过有效学习、签到、成就等方式获取。奖励结算必须由服务端完成，客户端不能直接决定最终金币、EXP 或成就。设计统一 `RewardService` 和 Reward Ledger/奖励流水。每个学习事件使用唯一幂等 ID，同一事件不得重复领取奖励。奖励相关更新必须使用事务，防止网络重试、离线同步或恶意请求造成重复发放。



【勋章系统】支持勋章墙、称号、金币奖励和宠物专属奖励。V1 实现勋章、称号、金币以及部分宠物表情/动作奖励，数据结构预留未来装扮奖励。支持普通成就和隐藏成就，例如首次学习、累计学习时间、掌握单词数量、连续学习天数、口语时长、听力完成量、宠物等级等。成就判断由服务端完成。



【词库系统】词库必须可扩展，不得把 CET-4/CET-6 写死在数据库和业务逻辑。V1 默认提供 CET-4 和 CET-6，未来后台可以新增考研、雅思、托福或自定义词库。Android 支持下载词库用于离线学习。单词基础数据包含单词、英式/美式音标、英式/美式发音、词性、核心释义、例句及翻译、常见短语、常见搭配、词形变化、近义词/反义词、四六级常考含义等。用户可以设置默认英音或美音。可靠词典数据和 AI 生成内容必须区分，AI 只能补充记忆技巧、更多例句、易混词比较和用法解释，不得覆盖基础词典数据。



【单词学习与复习】新词首次采用卡片式学习，之后根据记忆状态使用多种复习题型，包括释义选择、英译中、中译英、拼写、听音辨词等。V1 使用 SM-2 类间隔重复算法，支持忘记/困难/一般/简单或由答题结果映射到复习评分。必须完整保存每次复习结果、时间、用时、正确率、调度参数和历史记录，为未来升级 FSRS 保留数据。不得未来升级 FSRS 时要求用户清空学习数据。提供今日复习、生词本、已掌握词汇、学习历史和统计。



【拼写与听写】提供独立拼写训练和听写训练。听写模式播放单词发音，用户输入英文。记录漏字母、重复字母、字母顺序错误等常见错拼信息，并合理提高这些单词后续复习权重。



【AI 配置】平台不承担用户的大模型调用费用，用户自行配置 AI API。文本模型、STT、TTS、Realtime/实时语音允许分别配置不同 Provider，不假设一家服务支持全部能力。优先支持 OpenAI-Compatible 接口，但通过 Provider 抽象允许未来扩展其他协议。AI 配置至少包括 Provider 类型、Base URL、API Key、模型名和必要参数。API Key 需要加密后保存到服务器，以支持 Web/Android 同步。数据库禁止保存明文 API Key。服务端使用独立主密钥进行 AEAD 加密，主密钥来自环境变量或独立密钥文件，禁止写入 Git。日志、异常信息、后台页面和遥测不能泄露完整 Key。提供连接测试，并分别测试文本/STT/TTS/Realtime 能力，不能仅测试一个接口就显示“全部正常”。



【AI 实时口语】V1 必须提供真正的实时语音口语体验，而不是只实现“录音→上传→等待回复”。支持情景口语、自由对话、CET-4/CET-6 口语专项。支持老师模式和朋友模式、实时字幕、流式 STT、模型流式响应、流式 TTS；Provider 能力允许时支持用户打断 AI 的 barge-in。Provider 不支持某项能力时需要优雅降级并明确告诉用户缺少什么能力，不能静默失败。纠错支持“即时纠错”和“结束后总结”两种模式，默认结束后总结。对话结束生成口语报告，至少包括发音、流畅度、语法、词汇、表达自然度、改进建议和推荐复习表达。报告属于学习反馈，不要假装是官方 CET 分数。用户可以把对话中出现的生词或表达加入个人复习列表。



【听力系统】同时提供“四六级听力”和“日常听力”。内容来源可以是合法授权的 CET 素材、许可证明确的开放资源、自制内容和 AI 生成的 CET 风格内容。禁止未经确认直接抓取、复制或打包版权状态不明的真题资源。统一播放器支持播放、暂停、进度、合理范围变速、逐句播放和循环。训练支持做题、答案解析、原文、逐句翻译、重点词汇、精听和听写。用户听错或不会的词/短语在练习结束后进入“待复习条目”，由用户选择是否加入生词本，禁止自动塞入大量词条。V1 实现 CET-4/CET-6 听力模拟考试，包含考试计时、连续播放、合理的考试限制以及结束后统一评分和解析。完整阅读、写作、翻译模考属于未来版本，不进入 V1。



【首次使用】注册成功后提供可跳过的英语水平测试。用户可以直接选择 CET-4 或 CET-6 开始学习，也可以先完成测试，由系统推荐学习起点和目标。测试是推荐工具，不是进入 App 的强制门槛。



【提醒】提供可自定义通知：每日学习提醒、当天未达标提醒、连续学习提醒、待复习单词提醒、宠物状态提醒。用户可单独开关并设置时间。Android 使用系统通知；Web 获得授权后使用浏览器通知。默认不要全部开启，禁止高频骚扰用户。



【Android 离线】Android 支持部分离线。已经下载的词库、新词学习、单词复习以及必要学习记录可以离线使用；AI 口语、人机验证和依赖在线资源的能力需要联网。设计本地 SQLite 或合适的 React Native 持久化存储与 Sync Queue。每个离线事件生成全局唯一 `eventId` 和客户端时间。恢复联网后通过服务端幂等 API 同步。必须考虑重复上传、乱序、服务端状态变化和同一账号多设备同时使用。金币、EXP、连续学习和勋章最终以服务端结算结果为准，不能信任客户端提交的最终数值。



【V1 不做】好友、排行榜、私聊、社区、动态、金币充值、多宠物、宠物房间装修、宠物复杂换装、Live2D、完整 CET 阅读/写作/翻译模拟考试、iOS、Windows 客户端均不在 V1 范围内。可以预留合理扩展点，但禁止提前实现大量未来功能或制造无意义抽象。



【开发阶段】严格按阶段开发。Phase 1：项目地基，完成 Monorepo、React Web、React Native Android、React Admin、NestJS、SQLite、数据库 migration、账号注册登录、人机验证抽象、恢复码、RBAC、API SDK、基础测试、环境检查和开发文档。Phase 2：单词核心，完成可扩展词库、CET-4/CET-6、新词学习、SM-2、复习、英美发音、拼写、听写、生词本、学习历史和统计。Phase 3：学习与宠物闭环，完成每日目标、学习积分、连续学习、金币、Reward Ledger、宠物 EXP、心情、饱食度、Sprite 动画和升级动作解锁。Phase 4：听力，完成日常听力、四六级听力、播放器、答题、精听、听写、逐句解析、错题/错词和 CET 听力模拟考试。Phase 5：AI 实时口语，完成 AI Provider、加密 API Key、STT/TTS/Realtime、实时字幕、打断、情景/自由/CET 模式、纠错和口语报告。Phase 6：长期成长，完成勋章、称号、隐藏成就、统计趋势和完整通知。Phase 7：完成 Android 离线同步、完整管理后台、安全审计、性能测试、数据库备份恢复、部署文档和正式发布准备。



【阶段规则】每个 Phase 开始前先写实施计划和验收标准。上一阶段有重大测试失败、核心功能未实现、数据模型明显错误或安全问题时，禁止偷偷进入下一阶段。每阶段结束必须输出：已完成内容、未完成内容、实际执行的测试命令和结果摘要、安全检查结果、重要技术决策、Git 提交情况和下一阶段计划。不要创建几十个只有标题和 mock 数据的空页面假装开发完成。



【工程规范】优先简单、可靠、可维护的实现，遵循 DRY、SOLID、YAGNI，但禁止为“架构漂亮”制造大量没有实际价值的抽象。NestJS 按领域合理拆分，例如 auth、users、vocabulary、review、study-plan、rewards、pets、achievements、listening、speaking、ai-config、sync、admin 等，具体结构可以根据实际项目调整。前后端 API 使用明确 DTO 和运行时验证。尽量避免 `any`。文件保持职责单一，不要创建巨大万能 Service 或上千行组件。Web、Mobile、Admin 不复制核心业务算法，核心规则放到可测试的共享模块或服务端。



【数据安全】金币、EXP、学习积分、成就、连续学习、复习调度等关键数据更新需要事务保护和幂等处理。客户端输入永远不可信。错误响应不能泄露内部堆栈、SQLite 路径、Token、API Key、服务器文件结构或环境变量。API Key、加密主密钥、JWT Secret 等禁止写进源码或提交 Git。提供 `.env.example`，不得提交真实 `.env`。



【测试要求】核心规则优先采用测试驱动或至少先定义可验证行为再实现。必须覆盖：注册登录、恢复码、Token 生命周期、管理员权限、SM-2 调度、学习积分、连续学习、Reward Ledger 幂等、金币/EXP 防重复结算、宠物状态、成就触发、AI 配置加解密、StorageProvider、文件上传安全、离线同步冲突、后台越权。服务端写单元测试和集成测试，关键 API 做 E2E；Web/React Native 的主要用户流程写组件或集成测试。不要只在回答里写“测试通过”，必须实际运行测试、lint、typecheck、build 等命令，并依据真实输出报告。



【安全审计】V1 完成前进行专门代码和安全审计，至少检查：依赖漏洞、硬编码秘密、恶意代码和恶意依赖、认证绕过、权限绕过、IDOR、SQL 注入、XSS、CSRF、SSRF、文件上传、目录穿越、暴力破解、API Key 泄漏、日志泄密、客户端伪造学习结果、奖励重复提交、离线同步重放、整数边界、管理员权限、危险网络请求和供应链风险。检查第三方依赖和素材许可证。禁止加入广告 SDK、挖矿、隐蔽遥测、未经说明的数据收集或与产品无关的后台网络行为。



【文档】持续维护 README、项目架构、目录说明、数据库设计、API 文档、开发环境配置、Android 构建、AI Provider 配置、素材许可证记录、生产部署、数据库备份恢复和安全说明。文档必须与实际实现保持一致，不允许生成一堆过期文档。



【现在的执行顺序】现在不要直接开始大量编码。第一步只做只读环境和仓库检查：确认当前 Git 仓库、Node/npm、Git、JDK、Android Studio/Android SDK/ADB 等实际状态，并列出检测结果。第二步阅读已有项目文件，如果是空仓库则明确说明。第三步根据上述要求创建产品设计/架构方案、目录规划、初始数据库 ER 设计和 Phase 1 实施计划。第四步检查计划是否存在需求冲突、过度设计或环境依赖问题。如果 Phase 1 需要 React、React DOM、React Native、NestJS 或其他正常 npm 项目依赖，可以直接在项目内安装；如果需要 React 之外的额外系统工具、SDK、运行环境或软件，而当前电脑没有安装，必须先告诉用户并等待用户命令，绝对不能擅自下载安装。完成环境检查和 Phase 1 计划后先向用户汇报，再进入实际 Phase 1 开发。



【最终原则】这是要真实长期维护的正式项目，不是一次性 Demo。任何功能必须能够真实运行、真实保存数据、真实同步和真实测试。遇到不确定问题优先检查文档、现有代码和实际环境，不要凭空猜测。发现需求存在明显技术风险时可以提出更可靠方案，但不要擅自删减已经确定的核心功能。